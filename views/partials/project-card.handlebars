<div class="card h-100">
  <div class="card-body d-flex flex-column">
    <h2 class="h6 mb-1">{{name}}</h2>
    <p class="text-muted small mb-2">by
      {{user.name}}
      •
      {{format_date date_created}}</p>
    <p class="mb-3 flex-grow-1">{{description}}</p>
    <div class="d-flex justify-content-between align-items-center">
      <span class="badge text-bg-warning">Needed: ${{format_amount
          needed_funding
        }}</span>
      <a class="btn btn-sm btn-outline-dark" href="/project/{{id}}">Details</a>
    </div>
  </div>
</div>